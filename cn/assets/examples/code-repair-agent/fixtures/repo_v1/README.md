# repo_v1 fixture

This repository fixture contains a deterministic failing `total` example. The
repair agent may inspect it and produce a patch artifact, but it never mutates
this directory or accesses a network.
